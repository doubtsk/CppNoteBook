# Const在函数前后的区别
```
const Sales_data* ptr;  // 可以改变ptr指向，但不能通过ptr修改对象
```

```
Sales_data *const ptr;  // 不能改变ptr指向，但可以通过ptr修改对象
```
# Const 在函数声明后的作用
```
std::string Mname() const { return name;} //不允许修改name这个值
```
# This 在 C++的作用
this 是 C++ 中的一个关键字，也是一个 const 指针，它指向当前对象，通过它可以访问当前对象的所有成员。
# 顶层 Const 和底层 Const 的区别
顶层 Const 代表修饰的是对象本身，保护变量本身永远不变
底层 Const 代表指针或引用的对象是 const，保护指向的数据不被修改，但允许指针指向其他地址

---
# 类 `Class` 和 `Struct` 的区别
- **结构体（struct）**：默认成员访问权限为 `public`。
- **类（class）**：默认成员访问权限为 `private`。
- **结构体（struct）**：通常用于纯数据结构，主要存储数据，成员通常是公开的。
- **类（class）**：用于包含数据和操作数据的函数，支持更加复杂的封装。
# `.` 和 `->` 的区别
`.` 是用来直接访问对象和函数。
`->` 是通过指针来访问对象，相当于 `(*ptr). member`
# 访问控制
- **public**：公有成员，可以被所有代码访问。
- **private**：私有成员，仅能被类的成员函数和友元访问。
- **protected**：受保护成员，仅能被类的成员函数、友元和派生类访问。
# 构造函数
构造函数的名字和类名相同。和其他函数不一样的是，构造函数没有返回类型
不同于其他成员函数，构造函数不能被声明成const的
<font color="#c00000">一个普通的类必须要定义构造函数</font>
```
self() : name(""), age(), weight() {}
self()=default;
//这两行其实是一样的，只不过第一行有更确切的默认值
```

# 引用
<font color="#c00000">作为引用，那么就一定要被初始化</font>
# 拷贝构造
- **定义**：用于创建一个新对象，并复制现有对象的成员。
- **语法**：`ClassName(const ClassName& other);`
- 基类的构造函数在派生类之前调用，析构函数则在派生类之后调用。
# 拷贝赋值构造
- **定义**：用于将一个已有对象的值赋给另一个已有对象。
- **语法**：`ClassName& operator=(const ClassName& other);` 
# 移动构造
- **移动语义（Move Semantics）**：允许资源的所有权从一个对象转移到另一个对象，避免不必要的拷贝，提高性能。另一个对象将自动创建
`ClassName(ClassName&& other) noexcept;`
[[#^adcac1]] `noexcept`
##  **移动赋值运算符**：
`ClassName& operator=(ClassName&& other) noexcept;`
允许资源的所有权从一个对象转移到另一个**已有**对象，避免不必要的拷贝，提高性能。
> Noexcept 只是代表你认为不会抛出异常的关键字，编译器检测到 noexcept 会有相应的性能优化，当你不确定是否有异常时，不要写(毕竟 cpp 的性能已经很强大了，没必要在这方面进行性能优化)
^adcac1
# 什么时候禁用拷贝和赋值构造呢？

- 首先，编译器默认生成的拷贝和赋值构造呢，都是浅拷贝(也就是只创建一个指针再指向这个内存) ，当调用析构的时候，如果浅拷贝了两个指针，那么析构就会对这两个指针进行释放两次内存，会造成未知后果，你预知你写的类会发生这种情况的时候，应当禁用拷贝和赋值构造。
- 当你的类内部利用了 `new` 开辟了内存，因为上方列出的原因，也会造成未知后果，你应当禁用拷贝和赋值构造。
- 你写的这个类是一个需要在某个时间必须独占某个资源时
- 单例模式

#  析构函数
- 析构函数（Destructor）：在对象生命周期结束时调用，用于释放资源。
# 友元
- **友元（Friend）**：可以访问类的私有和保护成员的非成员函数或另一个类。
- 语法: `friend`

> 非成员函数: 在 C++ 中，非成员函数是指不属于任何类的函数。可以类的外面自由定义，一般是用来操作变量的。

## 运算符重载

## 什么是运算符重载

- **运算符重载（Operator Overloading）**：允许对自定义类型使用C++运算符，如 `+`, `-`, `<<` 等。
# 重载 operator 的原因
##  &operator<< 
- 重载这个，是为了与一些 STL 和第三方库无缝协作
- 支持任意输出流 (文件流，字符流)
- 泛型编程需求
## 重载运算符的规则

- 只能对已有运算符进行重载，不能创建新运算符。
- 至少有一个操作数必须是用户定义的类型。
- 改变不了运算符的优先级或结合性。
- `operator` 后跟的符号用来告诉编译器需要去重载指定的operator 运算符。

# 单例
- 单例模式(Singleton Pattern)是一种创建型设计模式，它确保一个类只有一个实例，并提供一个全局访问点、
- 为了防止被 `new xxx()` 实例化，所以<font color="#c00000">无论是以什么模式启动</font>，都需要把**构造函数、析构函数、拷贝函数、赋值函数**全部私有化，
- 用静态变量存储唯一实例(因为静态变量属于类，生命周期不依赖于任何对象，为程序的生命周期)
- 提供一个公共的静态获取实例的方法，通常是 `GetInstance()` 用于全局访问这个唯一实例
## 饿汉模式 (在程序启动时就创建实例，无论是否需要)
```
class EagerSingleton {
private:
    static EagerSingleton instance;  // 静态实例
    EagerSingleton() {}
    ~EagerSingleton() {}
    EagerSingleton(const EagerSingleton&) = delete;
    EagerSingleton& operator=(const EagerSingleton&) = delete;

public:
    static EagerSingleton& getInstance() {
        return instance;
    }
};
// 在程序开始前就已经初始化
EagerSingleton EagerSingleton::instance;
```
- 线程安全：因为实例在main函数之前就已经初始化完成
- 可能会浪费资源：如果实例从未被使用
- 没有延迟初始化的好处
## 懒汉模式(在第一次被请求时才创建实例)
```
class LazySingleton {
private:
    static LazySingleton* instance;
    LazySingleton() {}
    ~LazySingleton() {}
    LazySingleton(const LazySingleton&) = delete;
    LazySingleton& operator=(const LazySingleton&) = delete;
public:
    static LazySingleton* getInstance() {
        if (instance == nullptr) {
            instance = new LazySingleton();
        }
        return instance;
    }
};
LazySingleton* LazySingleton::instance = nullptr;
```
- 资源利用高效：只有需要时才创建实例
- 线程不安全：需要额外处理多线程环境
- 实现稍复杂

> ==分辨是饿汉还是懒汉，快速的分辨方法就是看单例实例被初始化成了**实例**(饿汉)还是**空指针**(懒汉)==

| 特性    | 饿汉模式          | 懒汉模式          |
| ----- | ------------- | ------------- |
| 初始化时机 | 程序启动时         | 第一次被使用时       |
| 线程安全  | 是             | 需要额外处理        |
| 资源占用  | 可能浪费          | 按需分配          |
| 性能    | 访问速度快         | 第一次访问可能有延迟    |
| 实现复杂度 | 简单            | 复杂(尤其是线程安全版本) |
| 适用场景  | 初始化耗时短，一定会被使用 | 初始化时间长，不一定被使用 |
- **控制实例生命周期 + 提供全局访问入口** 这句话就是单例的核心思想，通过这个思想，那么会用到单例的例子就是这几个:日志、配置、连接池。
# 类继承
- 是面向对象编程（OOP）中的一个核心概念，允许一个类（派生类）从另一个类（基类）继承属性和行为。通过继承，派生类可以重用基类的代码，并根据需要添加新的成员或重写现有成员。
# 虚函数
- 允许派生类重新定义基类中的函数，以实现多态性。在运行时，根据对象的实际类型调用相应的函数版本。
# 继承后的访问控制（Access Control in Inheritance）
- 继承时的 **访问控制** 决定了基类成员在派生类中的可访问性。继承方式主要有三种：`public`、`protected` 和 `private`。它们影响继承成员的访问级别。
## 语法与影响

- **公有继承（public inheritance）**：
- 基类的 `public` 成员在派生类中保持 `public`。
- 基类的 `protected` 成员在派生类中保持 `protected`。
- 基类的 `private` 成员在派生类中不可访问。
- **保护继承（protected inheritance）**：
- 基类的 `public` 和 `protected` 成员在派生类中都变为 `protected`。
- **私有继承（private inheritance）**：
- 基类的 `public` 和 `protected` 成员在派生类中都变为 `private`。
# 想删除某些构造?
如果想要禁用掉类里面的某些构造，有两种写法，一种是 c 11 之前的: 直接将这个构造写成私有即可，c 11 以后的，直接"=delete"即可，(不写，编译器会自动构造一个默认的)

---
# 内存管理

## 内存布局
### 栈
- 由编译器自动管理，存储局部变量和函数参数
- 生命周期随着函数调用创建和销毁
- 速度快，大小受限(通常在几 MB)
### 堆
手动管理内存，用于动态分配内存。内存分配和释放由程序员控制，灵活但易出错
### 全局/静态区
存储全局变量和静态变量

## 内存对齐原由以及解释
- 要搞明白内存对齐，就要明白在硬件设计上，数据读取的处理器只能从地址为k的倍数的内存处开始读取数据。这种读取方式相当于将内存分为了多个"块“，假设内存可以从任意位置开始存放的话，数据很可能会被分散到多个“块”中，处理分散在多个块中的数据需要移除首尾不需要的字节，再进行合并，非常耗时。

- 对于基础类型，如 `float, double, int, char` 等，它们的大小和内存占用是一致的。而对于结构体而言，如果我们取得其sizeof的结果，会发现这个值有可能会大于结构体内所有成员大小的总和，这是由于结构体内部成员进行了内存对齐。
> 这里"它们的大小和内存占用是一致的"指的是**例如 `int` 的 `sizeof` 是 4，那它实际就占用 4 字节，不会因为对齐被扩充到 8 字节。**

> 某些平台内存不对齐，会导致崩溃

## 内存对齐规则
> 这里只关注类的内存对齐规则

**成员的排列顺序会影响结构体的总大小**
举个例子:
```
struct A
{
    int i;
    char c1;
    char c2;
};
 
struct B
{
    char c1;
    int i;
    char c2;
};
 
int main()
{
    cout << sizeof(A) << endl; // 有效对齐值为4, output : 8
    cout << sizeof(B) << endl; // 有效对齐值为4, output : 12
    return 0;
}
```
会发现虽然 `A` 和 `B` 的成员类型完全相同，只是顺序不同，但 `sizeof(A) == 8`，而 `sizeof(B) == 12`。这是由 **对齐规则** 和 **填充字节（Padding）** 导致的。
![[Pasted image 20250616190043.png]]
> 空白就是填充字节，这张图很明显的展示了为什么虽然 `A` 和 `B` 的成员类型完全相同，只是顺序不同，但 `sizeof(A) == 8`，而 `sizeof(B) == 12` 的问题

在大多数系统（如 x 86-64）中，对齐规则如下：
1. **每个成员的地址必须是对齐值的整数倍**。
    - `char` 对齐值 = 1（任意地址均可）
    - `int` 对齐值 = 4（地址必须是 4 的倍数，如 `0x1000`, `0x1004`）
2. **结构体的总大小必须是最宽成员对齐值的整数倍**
3. **编译器会在成员之间插入填充字节（Padding）以满足对齐要求**

# 内存管理操作

## `new`
- 用法:
```Type* ptr = new Type(parameters);```
- 作用:
	- 负责在堆上为对象分配内存。
    - 调用对象的构造函数（如果有）来初始化对象。
    - 返回一个指向新分配和初始化的对象的指针。
## `delete`
- 用法
	- 释放单个对象 `delete ptr;`
	- 释放数组 `delete[] ptr;`
- 作用:
- 负责释放通过 `new` 操作符在堆上分配的内存。
- 调用对象的析构函数（如果有）来清理对象。
- 使指针变为空指针（即指向 `nullptr`）。

| 特性         | Malloc/free                    | New/delete                    |
| ---------- | ------------------------------ | ----------------------------- |
| 类型安全       | 需要显式类型转换                       | 自动类型转换，无需显式转换                 |
| 构造/析构函数    | 不调用对象的构造/析构函数                  | 调用对象的构造/析构函数                  |
| 返回值        | `void*`，需要转换为目标类型              | 返回目标类型指针，类型安全                 |
| 错误处理       | 分配失败返回 `nullptr`               | 分配失败抛出 `std::bad_alloc` 异常    |
| 多态行为       | 无                              | 支持多态，通过虚函数正确调用析构函数            |
| 内存分配与释放对应性 | 必须使用 `free` 释放由 `malloc` 分配的内存 | 必须使用 `delete` 释放由 `new` 分配的内存 |
## `realloc` 内存重分配关键字
`realloc` 用于调整之前分配的内存块大小。

用法:
`Void* realloc (void* ptr, size_t new_size);`
> 这里的 `void *` 指的是接受任意类型

> 注意，realloc 无法重分配由 new 分配的内存空间，因为 `new` 和 `delete` 机制过于复杂，单纯的移动内存块会导致与C++对象生命周期管理不兼容，若要重分配，请将数组改成 `vector` 类型，而其余的方法不再过多介绍。

# RAII(Resource Acquisition Is Initialization)资源获取即初始化
-  `RAII` 是 C++ 中的一种编程惯用法，通过对象的生命周期管理资源，确保资源在对象构造时获取，析构时释放，避免泄漏。
## RAII 的最主要的实现: 智能指针
### `std::shared_ptr`
它是 C++ 标准库中功能强大的智能指针之一，提供了共享所有权的能力，使得多个指针可以共同管理同一个动态分配的对象。通过引用计数机制，`shared_ptr` 确保了对象在最后一个指针被销毁时自动释放，极大地简化了内存管理，防止了内存泄漏和悬挂指针问题。

用法:
```

std::shared_ptr<A> ptrA;//声明
std::shared_ptr<A> a = std::make_shared<A>();//利用std::make_shared进行创建
```
### `std::weak_ptr`
它主要是用来解决 `std::shared_ptr` 在双向关联的情况下可能会循环引用导致的内存泄露的问题，它没有所有权机制。

> 若要通过 `std::weak_ptr` 访问变量，请调用 `lock()` 进行提权，该函数会返回一个有效的 `std::shared_ptr` ，若 `std::shared_ptr` 被 `reset()` 释放，则返回空指针。

用法:
```
std::weak_ptr<A> ptrA;//声明
std::shared_ptr<A> a = std::make_shared<A>();//创建的方法和std::shared_ptr一模一样
```
### `std::unique_ptr`
它是 C++ 标准库中功能强大的智能指针之一，提供了独享权的能力，该指针被禁用了拷贝，它只被允许移动。这样的特性保证了 `unique_ptr` 的独占权能力。
用法:
```
std::unique_ptr<A> ptrA(new A());//cpp11标准下
std::unique_ptr<A> ptrA=std::make_unique<A>();//cpp14标准及以上
```

# Lambda 表达式
语法:
```
[captures](parameters) -> return_type {
    // 函数体
}
```
> - **captures**: 捕获外部变量的方式，可以是值捕获、引用捕获或者混合捕获。
> - **parameters**: 参数列表。
> - **return_type**: 返回类型，可以省略，编译器会自动推导。
> - **函数体**: 实际执行的代码。

个人理解为是一种简化的函数体，又因为是一种简化的函数体，所以它几乎可以使用在任何一种场景下
✅ **STL 算法**（排序、过滤、转换）  
✅ **多线程/异步任务**（线程、回调）  
✅ **事件处理**（GUI、网络）  
✅ **泛型编程**（`auto` 参数）  
✅ **替代小型函数对象**（比较器、状态保持）
> 这里是 DeepSeek 给出的结果
# 左值和右值
**左值（lvalue）**和**右值（rvalue）**是C++中用于描述表达式值类别的重要概念。
- **左值（lvalue）**：
- 表示具有持久存储的对象。
- 可以出现在赋值语句的左侧。
- 可以被取地址（即，可以使用`&`运算符）。
- 示例：变量名、引用等。
- **右值（rvalue）**：
- 表示临时对象或没有持久存储的值。
- 通常出现在赋值语句的右侧。
- 不能被取地址。
- 示例：字面量、临时对象、表达式结果等。
C++11进一步细化了右值的分类：
- **纯右值（prvalues）**：表示临时对象或字面量，如`42`、`3.14`。
- **将亡值（xvalues，expiring values）**：表示即将被移动的对象，如 `std::move` 的结果。

那这有啥鸟用呢？
## 原样转发
