
对于指针这个问题，其实对于操作底层的 c\cpp 代码的程序员，其实是很致命的问题，因为 c\cpp 的思想就是"**相信程序员的代码能力，不管这对不对，先运行吧。**"，但是事与愿违，现在的程序员只能说一言难尽，至少我身边的几位都对指针这个东西头疼不已。我也曾经头疼过，但是现代的 cpp, 也就是 C11 标准下诞生的智能指针，就是来解决——"我到底释放了指针？还是没释放？还是莫名其妙释放两次"的问题，但先上来就讲智能指针，无异于喝果粒橙只喝饮料不喝果粒。所以我们得从最基础的走起——裸指针。

> ==所有的内容，均基于 C11 标准。==
# 裸指针
- 语法: `类型 变量*`
讲起来其实没什么意思，~~毕竟你都学 C\CPP 了，你要是不懂指针，建议看看基础 C\CPP，那个比较适合你~~
这里主要讲讲程序员因为这个指针会犯的几个搞笑错误，~~别笑，你也会犯~~
- 我释放了吗？
	指的是写了类，但是由于某些问题 (比如提前 break 或者 return)，导致没走析构函数释放指针，或者直接就是写了个指针，但是到程序最后都没看到一个 delete。
- 我怎么释放了两次??
	指的是写嗨了，两个指针指向一个内存，导致走析构函数或者是 delete 两个指针时，第一次内存被正确释放，第二次又释放了那块内存，这会导致未知的错误(比如那块内存在释放后接着又放入了重要数据，结果程序 boom 了)。

因此智能指针出现了，它智能在哪了呢？主要是你不管怎么写，它会完全管理资源的释放，无论你的程序怎么跑，它一定保证资源会正确释放。

# RAII (Resource Acquisition Is Initialization) 资源获取即初始化
-  `RAII` 是 C++ 中的一种编程惯用法，通过对象的生命周期管理资源，确保资源在对象构造时获取，析构时释放，避免泄漏。

好，有了这些知识基础，接下来理解智能指针就会好很多了

# 智能指针
- 智能指针体现在把裸指针进行了一次面向对象的封装，在构造函数中初始化资源地址，在析构函数中负责释放资源
- 利用栈上的对象出作用域自动析构这个特点，在智能指针的析构函数中保证释放资源

你眨眼一看，吔？这不就类嘛？笼统的说，确实是这样的。

但是你会发现一个问题：智能指针再怎么智能，他也是对裸指针的一次封装，只是利用 RAII 这个理念，解决了资源的自动释放，但是像什么，指针的浅拷贝啊，多个智能指针指向一个内存的时候，不还是会多次释放嘛？

是的，你想得到，那些大佬也自然能想的到，接下来这几个智能指针，就是共同来解决这些问题的，

##  auto_ptr

> 该指针因严重的所有权转移问题，已在 c11 标准下被遗弃，C 17 标准下删除，请勿使用

看源码吧
~~~template<class _Ty>
	Class auto_ptr
	{	// wrap an object pointer to ensure destruction
Public:
	Typedef _Ty element_type;

	Explicit auto_ptr (_Ty * _Ptr = nullptr) noexcept
		: _Myptr (_Ptr)
		{	// construct from object pointer
		}

	/*这里是 auto_ptr 的拷贝构造函数，_Right.Release () 函数中，把_Right 的_Myptr 赋为 nullptr，也就是换成当前 auto_ptr 持有资源地址*/
	Auto_ptr (auto_ptr& _Right) noexcept
		: _Myptr (_Right.Release ())
		{	// construct by assuming pointer from _Right auto_ptr
		}
		
	_Ty * release () noexcept
		{	// return wrapped pointer and give up ownership
		_Ty * _Tmp = _Myptr;
		_Myptr = nullptr;
		Return (_Tmp);
		}
Private:
	_Ty * _Myptr;	// the wrapped object pointer
};
~~~
这个指针，它怎么解决浅拷贝和多次释放的呢？如果发现有多个指针，将之前的所有 auto_ptr 指针全部置为 nullptr，永远使得最后一个 auto_ptr 类型指针指向内存
哈哈，很粗暴对不对，所以被废弃了，C++强烈不建议这么做，除非你的场景十分简单。

##  scoped_ptr

源码：
```
template<class T> class scoped_ptr // noncopyable
{
Private:
    T * px;
	
	/*私有化拷贝构造函数和赋值函数，这样 scoped_ptr 的智能指针对象就不支持这两种操作，从根本上杜绝浅拷贝的发生*/
    Scoped_ptr (scoped_ptr const &);
    Scoped_ptr & operator=(scoped_ptr const &);
 
    typedef scoped_ptr<T> this_type;
		
	/*私有化逻辑比较运算符重载函数，不支持 scoped_ptr 的智能指针对象的比较操作*/
    Void operator==( scoped_ptr const& ) const;
    Void operator!=( scoped_ptr const& ) const;
 
Public:
    Typedef T element_type;
    Explicit scoped_ptr ( T * p = 0 ) : px ( p ) // never throws
    {
#if defined (BOOST_SP_ENABLE_DEBUG_HOOKS)
        Boost:: sp_scalar_constructor_hook ( px );
#endif
    }
 
#ifndef BOOST_NO_AUTO_PTR
	/*支持从 auto_ptr 构造一个 scoped_ptr 智能指针对象，但是 auto_ptr 因为调用 release () 函数，导致其内部指针为 nullptr*/
    explicit scoped_ptr ( std::auto_ptr<T> p ) BOOST_NOEXCEPT : px ( p.release () )
    {
#if defined (BOOST_SP_ENABLE_DEBUG_HOOKS)
        Boost:: sp_scalar_constructor_hook ( px );
#endif
    }
 
#endif
	/*析构函数，释放智能指针持有的资源*/
    ~scoped_ptr () // never throws
    {
#if defined (BOOST_SP_ENABLE_DEBUG_HOOKS)
        Boost:: sp_scalar_destructor_hook ( px );
#endif
        Boost:: checked_delete ( px );
    }
};
```
你会发现，它直接将赋值函数和拷贝函数私有化了，也就是禁用了拷贝和赋值操作，也就解决了浅拷贝问题。
因此，你一旦使用这个指针，就代表这个资源所有权不允许被转移。也就告诉阅读者，这个指针指向的资源，只在这个区域有效，出了这个区域，会自动释放
所以你要是在一些容器（map, vector）使用这个指针，会直接编译错误。
但你真的要让这个资源在这个区域有效且不允许被转移，其实也不应该用 `scoped_ptr` 指针，你更应该用下面那个。

##  unique_ptr
```template<class _Ty,
	class _Dx>	// = default_delete<_Ty>
	class unique_ptr
		: public _Unique_ptr_base<_Ty, _Dx>
	{	// non-copyable pointer to an object
public:
	typedef _Unique_ptr_base<_Ty, _Dx> _Mybase;
	typedef typename _Mybase::pointer pointer;
	typedef _Ty element_type;
	typedef _Dx deleter_type;

	/*提供了右值引用的拷贝构造函数*/
	unique_ptr(unique_ptr&& _Right) noexcept
		: _Mybase(_Right.release(),
			_STD forward<_Dx>(_Right.get_deleter()))
		{	// construct by moving _Right
		}
	
	/*提供了右值引用的operator=赋值重载函数*/
	unique_ptr& operator=(unique_ptr&& _Right) noexcept
		{	// assign by moving _Right
		if (this != _STD addressof(_Right))
			{	// different, do the move
			reset(_Right.release());
			this->get_deleter() = _STD forward<_Dx>(_Right.get_deleter());
			}
		return (*this);
		}

	/*
	交换两个unique_ptr智能指针对象的底层指针
	和删除器
	*/
	void swap(unique_ptr& _Right) noexcept
		{	// swap elements
		_Swap_adl(this->_Myptr(), _Right._Myptr());
		_Swap_adl(this->get_deleter(), _Right.get_deleter());
		}

	/*通过自定义删除器释放资源*/
	~unique_ptr() noexcept
		{	// destroy the object
		if (get() != pointer())
			{
			this->get_deleter()(get());
			}
		}
	
	/*unique_ptr提供->运算符的重载函数*/
	_NODISCARD pointer operator->() const noexcept
		{	// return pointer to class object
		return (this->_Myptr());
		}

	/*返回智能指针对象底层管理的指针*/
	_NODISCARD pointer get() const noexcept
		{	// return pointer to object
		return (this->_Myptr());
		}

	/*提供bool类型的重载，使unique_ptr对象可以
	直接使用在逻辑语句当中，比如if,for,while等*/
	explicit operator bool() const noexcept
		{	// test for non-null pointer
		return (get() != pointer());
		}
    
    /*功能和auto_ptr的release函数功能相同，最终也是只有一个unique_ptr指针指向资源*/
	pointer release() noexcept
		{	// yield ownership of pointer
		pointer _Ans = get();
		this->_Myptr() = pointer();
		return (_Ans);
		}

	/*把unique_ptr原来的旧资源释放，重置新的资源_Ptr*/
	void reset(pointer _Ptr = pointer()) noexcept
		{	// establish new pointer
		pointer _Old = get();
		this->_Myptr() = _Ptr;
		if (_Old != pointer())
			{
			this->get_deleter()(_Old);
			}
		}
	/*
	删除了unique_ptr的拷贝构造和operator=赋值函数，
	因此不能做unique_ptr智能指针对象的拷贝构造和
	赋值，防止浅拷贝的发生
	*/
	unique_ptr(const unique_ptr&) = delete;
	unique_ptr& operator=(const unique_ptr&) = delete;
	};
```
- 你会发现 unique_ptr 和 scoped_ptr 一样，都禁用了赋值和拷贝构造，但是，不同的是，unique_ptr 提供了带右值引用参数的拷贝构造和赋值，也就是说，unique_ptr 智能指针可以通过右值引用进行拷贝构造和赋值操作，或者在产生 unique_ptr 临时对象的地方，如把 unique_ptr 作为函数的返回值时。
-  unique_ptr 还提供了 reset 重置资源，swap 交换资源等函数，也经常会使用到。可以看到，unique_ptr 从名字就可以看出来，最终也是只能有一个该智能指针引用资源，因此建议在使用不带引用计数的智能指针时，可以优先选择 unique_ptr 智能指针

**你会发现，讲到现在，都是只能一个指针拿到资源的所有权，但我硬要多个指针指向一个资源呢？**

##  shared_ptr
这里主要介绍 shared_ptr 和 weak_ptr 两个智能指针，也就是带引用计数的智能指针，当允许多个智能指针指向同一个资源的时候，每一个智能指针都会给资源的引用计数加 1，当一个智能指针析构时，同样会使资源的引用计数减 1，这样最后一个智能指针把资源的引用计数从 1 减到 0 时，就说明该资源可以释放了，由最后一个智能指针的析构函数来处理资源的释放问题，这就是引用计数的概念。

要对资源的引用个数进行计数，那么大家知道，对于整数的++或者--操作，它并不是线程安全的操作，因此 shared_ptr 和 weak_ptr 底层的引用计数已经通过 CAS (**Compare And Swap**)操作，保证了引用计数加减的原子特性，因此 shared_ptr 和 weak_ptr 本身就是线程安全的带引用计数的智能指针。

Shared_ptr 智能指针的资源引用计数器在内存的 heap 堆上。Shared_ptr 一般被称作强智能指针，weak_ptr 被称作弱智能指针，它们有下边两个非常重要的应用场景需要注意：
- 智能指针的交叉引用问题
- 多线程访问共享对象问题
对于第一个问题，来个示例代码

```
#include <iostream>
#include <memory>
using namespace std;

class B; // 前置声明类B
class A {
public:
	A() { cout << "A()" << endl; }
	~A() { cout << "~A()" << endl; }
	shared_ptr<B> _ptrb; // 指向B对象的智能指针
};
class B {
public:
	B() { cout << "B()" << endl; }
	~B() { cout << "~B()" << endl; }
	shared_ptr<A> _ptra; // 指向A对象的智能指针
};
int main() {
	shared_ptr<A> ptra(new A());// ptra指向A对象，A的引用计数为1
	shared_ptr<B> ptrb(new B());// ptrb指向B对象，B的引用计数为1
	ptra->_ptrb = ptrb;// A对象的成员变量_ptrb也指向B对象，B的引用计数为2
	ptrb->_ptra = ptra;// B对象的成员变量_ptra也指向A对象，A的引用计数为2

	cout << ptra.use_count() << endl; // 打印A的引用计数结果:2
	cout << ptrb.use_count() << endl; // 打印B的引用计数结果:2

	/**
	 * 出main函数作用域，ptra和ptrb两个局部对象析构，分别给A对象和B对象的引用计数从2减到1，
	 * 达不到释放A和B的条件（释放的条件是A和B的引用计数为0），因此造成两个new出来的A和B对象
	 * 无法释放，导致内存泄露，这个问题就是“强智能指针的交叉引用(循环引用)问题”
	 **/
	return 0;
}
```
你在运行的时候会发现，资源并没有被成功释放，这就是交叉引用的问题，所以这里就会引出强弱智能指针的一个重要应用规则：定义对象时，**用强智能指针 shared_ptr，在其它地方引用对象时，使用弱智能指针 weak_ptr。**

##  weak_ptr
弱智能指针 weak_ptr 区别于 shared_ptr 之处在于：
1. Weak_ptr 不会改变资源的引用计数，只是一个观察者的角色，通过观察 shared_ptr 来判定资源是否存在
2. Weak_ptr 持有的引用计数，不是资源的引用计数，而是同一个资源的观察者的计数
3. Weak_ptr 没有提供常用的指针操作（如 `*` 和 `->` 运算符重载），无法直接访问资源，需要先通过 lock 方法提升为 shared_ptr 强智能指针，才能访问资源

但是也不要以为，我使用智能指针就一定不会出错，记住一件事，使用不当，瑰宝也是毒药。

```
int *p = new int;
shared_ptr<int> ptr1(p);
shared_ptr<int> ptr2(p);
cout << ptr1.use_count() << endl;
cout << ptr2.use_count() << endl;
```
你猜猜这段代码会不会报错？
因为 ` shared_ptr<int> ptr 1 (p) ` 和 `shared_ptr<int> ptr 2 (p)` 都调用了 shared_ptr 的构造函数，在它的构造函数中，都重新开辟了引用计数的资源，导致 ptr 1 和 ptr 2 都记录了一次 new int 的引用计数，都是 1，析构的时候它俩都去释放内存资源，导致释放逻辑错误，如下图所示：

![[Pasted image 20260327204119.png]]
你看，按照他们两个指针的释放逻辑，是没有任何错误的，但是一看，其实是错的。
因为每次创建，都是重新调用 shared_ptr 构造函数，所以，虽然管理的都是同一块内存，但是_Req 指向了不同的引用计数对象，因此析构错误。

那要怎么修正呢？

其实也简单啊，既然我分别调用了两次 `shared_ptr` 的构造，那我直接让指针 B 拷贝构造指针 A 不就好了？
```
int main() {
	A *p = new A(); // 裸指针指向堆上的对象

	shared_ptr<A> ptr1(p); // 用shared_ptr智能指针管理指针p指向的对象
	shared_ptr<A> ptr2(ptr1); // 用ptr1拷贝构造ptr2
	// 下面两次打印都是2，最终随着ptr1和ptr2析构，资源只释放一次，正确！
	cout << ptr1.use_count() << endl; 
	cout << ptr2.use_count() << endl;

	return 0;
}
```

但如果我想要让类中有一个函数能够返回这个类的 shared_ptr 智能指针呢？比如以下代码：
```
#include <iostream>
using namespace std;
// 智能指针测试类
class A {
public:
	A() : m_ptr(new int) { cout << "A()" << endl; }
	~A() {
		cout << "~A()" << endl;
		delete m_ptr; 
		m_ptr = nullptr;
	}
	 
	// A类提供了一个成员方法，返回指向自身对象的shared_ptr智能指针。
	shared_ptr<A> getSharedPtr() { 
		/*注意：不能直接返回this，在多线程环境下，根本无法获知this指针指向
		的对象的生存状态，通过shared_ptr和weak_ptr可以解决多线程访问共享*/
		return shared_ptr<A>(this); 
	}
private:
	int *m_ptr;
};
int main() {
	shared_ptr<A> ptr1(new A());
	shared_ptr<A> ptr2 = ptr1->getSharedPtr();

	/* 按原先的想法，上面两个智能指针管理的是同一个A对象资源，但是这里打印都是1
	导致出main函数A对象析构两次，析构逻辑有问题*/
	cout << ptr1.use_count() << endl; 
	cout << ptr2.use_count() << endl;

	return 0;
}
```
你要是上来就创建个函数，然后直接返回 shared_ptr，其实犯了和上面一摸一样的错误，你想啊，你这个类创建了一个 shared_ptr, 然后返回值又是一个 shared_ptr, 那么不还是会各自调用一次 shared_ptr 构造，然后管理的都是同一块内存，但是_Req 指向了不同的引用计数对象，最后导致析构错误嘛？

所以这里就要引出新的概念了
##  enable_shared_from_this
你只要对你想要返回智能指针的类继承这个东西，就能删掉那个用于返回智能指针的函数。
- 那么他是怎么实现的呢？
	只要类继承 `enable_shared_from_this`，那么创建对象的时候，对象会自动继承一个叫做 `_Wptr` 的变量，当你调用构造智能指针的函数时，自动将这个 `_Wptr` 变成一个弱智能指针的观察者。那么这还没完，还有与之配套的东西，叫做 `share_from_this`

##  shared_from_this

我们上面讲到，只要类继承 `enable_shared_from_this`，那么创建对象的时候，对象会自动继承一个叫做 `_Wptr` 的变量，但是这个只是一个观察者啊？不能作为智能指针啊？
所以这个就来了。当你调用这个函数的时候，会自动利用 `_Wptr` 变量进行提权，变成 `shared_ptr`，因此，你会发现，我没有调用任何的 `shared_ptr` 构造，但是成功创建了一个智能指针，不同的是，这个指针不会错误的释放资源，因为是通过类继承 `enable_shared_from_this` 这个基类，利用弱指针创建的。

##  make_shared
当你发现在外部需要延长类的生命周期的时候，记得使用这个，它会找到继承 `enable_shared_from_this` 类，然后调用 `shared_from_this` 进行构建 shared_ptr
指针。
